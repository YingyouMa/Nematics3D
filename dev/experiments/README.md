# Experiments

This directory contains exploratory implementations whose numerical behavior
and public API are still being evaluated.

- Keep each feature in its own subdirectory.
- Do not import experimental modules from `src/nematics3d`.
- Large input and output data should remain outside the repository.
- Once an experiment is stable, move its implementation and tests into the
  appropriate package under `src/nematics3d` and `tests`.

